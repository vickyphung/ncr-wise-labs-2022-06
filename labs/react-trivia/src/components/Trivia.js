function Trivia(props) {
    return (
        <div>
            {/* {props.trivia.map((question, index) => (
                <div>
                    <div>
                        {props.question.question}
                        {props.question.category.title}
                        {props.question.value}
                    </div>

                    <div className="answer">
                        {props.question.answer}
                    </div>

                </div>
            ))} */}
        </div>

    )
}

export default Trivia;